import PropTypes from 'prop-types';

export default {
  type: PropTypes.shape({
    id: PropTypes.number,
    ntype: PropTypes.string,
    authors: PropTypes.string,
    authors_h: PropTypes.string,
    forma: PropTypes.string,
    forma_h: PropTypes.string,
    genus: PropTypes.string,
    genus_h: PropTypes.string,
    hybrid: PropTypes.bool,
    is_basionym: PropTypes.bool,
    is_isonym: PropTypes.bool,
    notes: PropTypes.string,
    nothoforma: PropTypes.string,
    nothoforma_h: PropTypes.string,
    nothosubsp: PropTypes.string,
    nothosubsp_h: PropTypes.string,
    proles: PropTypes.string,
    publication: PropTypes.string,
    referenceToTypeDesignation: PropTypes.string,
    species: PropTypes.string,
    species_h: PropTypes.string,
    subsp: PropTypes.string,
    subsp_h: PropTypes.string,
    subvar: PropTypes.string,
    subvar_h: PropTypes.string,
    tribus: PropTypes.string,
    typeLocality: PropTypes.string,
    typification: PropTypes.string,
    unranked: PropTypes.string,
    var: PropTypes.string,
    var_h: PropTypes.string,
    vernacular: PropTypes.string,
  }),
  defaults: {
    id: null,
    ntype: null,
    authors: null,
    authors_h: null,
    forma: null,
    forma_h: null,
    genus: null,
    genus_h: null,
    hybrid: false,
    is_basionym: false,
    is_isonym: false,
    notes: null,
    nothoforma: null,
    nothoforma_h: null,
    nothosubsp: null,
    nothosubsp_h: null,
    proles: null,
    publication: null,
    referenceToTypeDesignation: null,
    species: null,
    species_h: null,
    subsp: null,
    subsp_h: null,
    subvar: null,
    subvar_h: null,
    tribus: null,
    typeLocality: null,
    typification: null,
    unranked: null,
    var: null,
    var_h: null,
    vernacular: null,
  },
};
